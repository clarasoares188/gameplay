import { StyleSheet } from "react-native";

export const stylesSignIn = StyleSheet.create({
    container: {
    flex: 1,
    backgroundColor: '#da60e7ff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});